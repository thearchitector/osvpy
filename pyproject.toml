[project]
name = "pyosv"
version = "0.1.0"
description = "Scan container images for vulnerabilities in Python. Built on OSV-Scanner."
readme = "README.md"
requires-python = ">=3.13"
license = "BSD-3-Clause-Clear"
dependencies = [
    "cvss>=3.6,<4",
    "pydantic>=2.12,<3",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Typing :: Typed",
  "Operating System :: POSIX :: Linux",
  "Operating System :: MacOS :: MacOS X",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
    "pytest-cov",
    "pytest-crap",
    "datamodel-code-generator",
]
local = [
    "mypy",
    "ruff",
    "prek",
]

[tool.datamodel-codegen]
input = "go/response.schema.json"
input-file-type = "jsonschema"
output = "src/pyosv/_generated.py"
output-model-type = "pydantic_v2.BaseModel"
target-python-version = "3.13"
disable-future-imports = true
disable-timestamp = true
use-standard-collections = true
use-union-operator = true
snake-case-field = true
allow-population-by-field-name = true
collapse-root-models = true
aliases = "go/model-aliases.json"
formatters = ["ruff-check", "ruff-format"]

[tool.pytest.ini_options]
strict = true
addopts = "-ra -vv --crap --cov pyosv --import-mode=importlib"
testpaths = ["tests"]
markers = ["integration: requires public registry and vulnerability database access"]

[tool.mypy]
explicit_package_bases = true
files = ["src/pyosv"]
strict = true
mypy_path = ["src", "stubs"]

[tool.ruff]
unsafe-fixes = false
extend-include = ["README.md"]

[tool.ruff.lint]
extend-select = ["ANN", "ASYNC", "PYI", "PT", "SIM", "TC", "TID", "PERF", "TRY", "PGH003"]
extend-ignore = ["TID252", "TRY003"]
preview = true

[tool.ruff.lint.flake8-type-checking]
strict = true
quote-annotations = true
exempt-modules = []
runtime-evaluated-base-classes = ["pydantic.BaseModel", "pydantic.RootModel", "pyosv._generated.FullScanData", "pyosv._generated.NativeResponse"]
runtime-evaluated-decorators = ["pydantic.computed_field"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
"importlib.import_module".msg = "Do not dynamically import modules. Explicitly import them at the top of the file."
"__future__.annotations".msg = "The annotation future statement is disallowed."

[tool.ruff.lint.isort]
split-on-trailing-comma = false

[tool.ruff.format]
docstring-code-format = true
skip-magic-trailing-comma = true
preview = true

[tool.scikit-build]
wheel.packages = ["src/pyosv"]
wheel.py-api = "py3"
wheel.exclude = ["**/*.h", "**/*.pyc", "**/__pycache__/**", "**/*.dSYM/**"]
sdist.inclusion-mode = "explicit"
sdist.include = ["pyproject.toml", "README.md", "go/**/*.go", "go/*.json", "go/go.mod", "go/go.sum", "src/pyosv/*.py", "src/pyosv/py.typed", "stubs/**/*.pyi", "CMakeLists.txt", "uv.lock"]

[build-system]
requires = ["scikit-build-core>=1.0.3,<1.1"]
build-backend = "scikit_build_core.build"
